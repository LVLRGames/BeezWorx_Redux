# BeezWorx Progress Log

| Date | Feature | Result | Bugs/Issues | Next Step |
| :--- | :--- | :--- | :--- | :--- |
| **03-14-2026** | Initialized LVLR G.D.B.L.A.S.T pipeline memory | Pipeline docs created | None | Draft Phase 1 Discovery Questions |
| **03-14-2026** | Ported barebones MVP scaffold to new repo | 17+ GDScript utilities drafted | None | Answer Phase 1 Questions, move to Phase 2 Link |
| **03-19-2026** | Phase 0 — Scaffold audit and world/colony boundary | 22 hex terrain files inventoried, boundary contract written, autoload order established | HexConsts static func fix, engine_time shader param registration | Commit hex terrain files, begin terrain scene |
| **03-19-2026** | Phase 0 — Integration test | 7/7 PASS | ColonyState stubs required implementation before T7 passed | Proceed to Phase 1: terrain scene + first visible world |
| **03-22-2026** | Phase 0 complete + Phase 1 World Scene | Full world scene running. Bee flyable with correct roll, pitch, yaw. Camera tracking via EventBus player_pawn_ready. Terrain, plants, grass, wind, bending, bounce all rendering. Plant lifecycle ticking via TimeService. Natural pollination and sprouting via PlantLifecycleSystem. | global_shader_parameter_get is editor-only — use remove+add pattern. HexTerrainManager must not call HexWorldState.initialize — WorldRoot owns init. sun_light.visible=false hides children including MoonLight — use energy lerp instead. ProceduralSkyMaterial has one sun_angle_max for all lights — use billboard quads for independent sun/moon disc control. day_phase=0.0 is dawn not midnight — no offset needed in arc math. | Begin PawnRegistry |
| **03-22-2026** | HiveSystem Phase 1 | Starter hive places at nearest tree to good biome start. Territory projects 127 cells (radius 6) with correct influence bands verified via debug label. HiveAnchorOccupant wires colony layer to world layer. | World start finder needed — origin is beach biome on seed 1738, no trees within radius 12. Fixed by querying HexWorldBaseline directly (no chunk dependency). bee_spawn_position must use adjacent empty cell not tree cell to avoid physics collision. | — |
| **03-22-2026** | TerritorySystem Phase 1 | Full influence model live. Step-valued influence (1.0/0.6/0.3) at correct distances. Fade on destruction. is_valid_expansion_cell working. Plant allegiance enum live. Save/load via hive recompute. | chunk_loaded signal must emit at END of on_chunk_loaded after cache is populated — querying cells before this returns empty. | — |
| **03-22-2026** | TimeService | Full implementation. day_changed, night_started, day_started, season_changed, year_changed all firing. get_time_string(), get_time_of_day_name(), get_current_season_name() working. current_world_time fully migrated out of HexWorldState. TimeService self-advances in _process. | HexTerrainManager must not advance time — TimeService owns the clock. EventBus-driven architecture replaces all await/polling patterns. | — |
| **03-22-2026** | DayNightCycle + Moon | Sun arc correct: dawn 0deg, noon -90deg, dusk -180deg. Sky colors blend through dawn/day/dusk/night driven by sun elevation angle not is_daytime bool. Moon is child DirectionalLight3D of sun with local X=180deg — inherits opposite arc for free. Billboard quad discs for sun and moon with independent size control via sun_angle_max swap trick. Depth-tested discs hide behind terrain correctly. Moon phase cycle over 28 days. | ProceduralSkyMaterial sun_angle_max controls ALL lights — swap value between sun/moon each frame to show correct disc per active body. visible=false on parent hides children — never use on sun since moon is child. disc_distance must be relative to camera position not world origin. | PawnRegistry |
